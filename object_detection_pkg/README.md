to run the image publisher:

    rosrun object_detection_pkg webcam_pub 0 # last digit is the camera index

to run the object detector on the camera feed:

    rosrun object_detection_pkg obj_det_sub.py
